# Neon Database (côté serveur pour migrations)
DATABASE_URL=postgresql://username:password@ep-example.region.aws.neon.tech/dbname?sslmode=require

# Neon Database (côté client - variable publique)
PUBLIC_DATABASE_URL=postgresql://username:password@ep-example.region.aws.neon.tech/dbname?sslmode=require