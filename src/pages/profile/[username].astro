---
import Layout from '../../layouts/Layout.astro';
import ProfilePage from '../../components/ProfilePage.tsx';

export async function getStaticPaths() {
  // Pour le mode statique, on génère quelques exemples
  return [
    { params: { username: 'marie_dubois' } },
    { params: { username: 'lucas_dev' } },
    { params: { username: 'emma_r' } },
    { params: { username: 'antoine_tech' } }
  ];
}

const { username } = Astro.params;
---

<Layout title={`${username} - Profil`}>
  <ProfilePage username={username} client:load />
</Layout>